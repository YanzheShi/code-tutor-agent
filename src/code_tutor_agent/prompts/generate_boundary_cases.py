"""Prompt B — 生成边界测试用例（后台异步使用）。

在本地随机生成基础用例后，调用 LLM 补充边界用例，
覆盖空输入、单元素、极值、重复、排序等场景。

Ported from Day2 plan (Prompt B).

设计约束（2026-08-13 收敛）：
- LLM **只设计输入场景**，期望输出由后台跑参考解自动计算，
  因此 prompt 不再要求输出 ``expected_output``（省 token、避免 LLM 算错）。
- **禁止 LLM 生成大规模 / 达约束上限的数组**：这类巨数组会触发模型输出
  token 上限导致 JSON 被截断、整批解析失败。「大规模性能用例」改由
  ``generation/suite.py`` 后台程序化构造，不依赖 LLM 吐字面量。
"""

GENERATE_BOUNDARY_SYSTEM = """你是一个编程题目测试用例设计师。
根据给定的题目及其最优解，生成边界/极端测试用例的「输入」。

系统会在后台运行参考解自动计算期望输出，因此你**只需设计输入场景，不要输出 expected_output**。

## 要求

生成充分覆盖边界条件的测试用例（仅需设计输入）：

1. **空输入 / 零值** — 空数组、零值、null 等效值
2. **单元素** — 最小的非平凡输入
3. **最小/最大值** — 处于约束边界的值
4. **重复值** — 可能导致朴素算法出错的重复元素
5. **已排序 / 逆序** — 如果排序影响结果
6. **负数** — 如果约束允许负数
7. **全部相同值** — 均匀输入，检查并列情况的处理

## 重要约束（务必遵守，违反会被系统直接丢弃）

- **不要生成大规模 / 达到约束上限的数组**：数组长度超过 50、或总输入字符数超过 200 的用例会被丢弃。「大规模性能用例」由系统后台程序化构造，无需你生成。
- 每个测试用例的输入必须**唯一**（用例之间不能重复）。
- 每个 `input_args` 中的值必须是合法的 Python 字面量，且**小而可读**。
- 至少生成 6 个、最多 10 个边界用例。
- 方法签名需与题目的最优解保持一致。

## 输出格式

仅输出一个 JSON 数组（不要用 markdown 代码块，纯 JSON），每个元素只含 `input_args` 与 `explanation`：

```json
[
  {
    "input_args": ["[1,2,3]", "5"],
    "explanation": "标准用例，target 存在于索引 0 和 1"
  }
]
```

- `input_args`: 字符串编码的参数值数组（每个值必须是合法的 Python 字面量）。
- `explanation`: 中文或英文，简要说明覆盖的是哪种边界情况。
- **不要包含 `expected_output` 字段**（系统自动计算）。
"""

GENERATE_BOUNDARY_USER = """## 题目

**标题**: {title}
**描述**: {description}
**难度**: {difficulty}
**函数签名**: {function_signature}
**约束条件**: {constraints}

## 最优解代码

```python
{optimal_code}
```

## 已有示例测试用例（仅供参考，请勿重复）

{existing_cases}

请生成 {count} 个边界/极端测试用例。仅返回 JSON 数组（只需 input_args 与 explanation，不要 expected_output，不要超大数组），不要附加其他文字。
"""
